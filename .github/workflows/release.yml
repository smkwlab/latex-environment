name: Build and Release PDF

on:
  pull_request_target:

permissions:
  contents: write

jobs:
  release:
    uses: smkwlab/latex-environment/.github/workflows/build-and-release.yml
    with:
# 以下の `document` を tex ファイルのファイル名から '.tex' を除いたものに変更
      file: document
